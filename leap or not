#include <stdio.h>

int main()
{
  int n;
  printf("Enter the valid year");
  scanf("%d",&n);
  if(n%4==0)
    printf("the year is a leap year");
  else if(n%400==0)
    printf("the year is a leap year");
    else
    printf("the year is a non leap year");
}
