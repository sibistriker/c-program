#include <stdio.h>

int main()
 {
   int n;
   float sum=0,x;
   printf("Enter the size \n");
   scanf("%d",&n);
   printf("Enter the numbers :\n");
   for(int i=0;i<n;i++)
   {
     scanf("%f",&x);
     sum=sum+x;
   }
   printf("The average of number is %f",sum/n);
 }
